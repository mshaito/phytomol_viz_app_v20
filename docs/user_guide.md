# User Guide

1. Install dependencies with `pip install -r requirements.txt`.
2. Run with `streamlit run app.py`.
3. Use the sidebar to move through the workflow.
4. Treat all built-in values as synthetic demonstration data.
5. Replace CSV files with real validated datasets only when available.
