# Developer Notes

- Keep app-wide logic in `app.py`.
- Keep each scientific workflow in a separate page under `pages/`.
- Keep reusable functions in `utils/`.
- Do not mix real experimental data with synthetic demo data without clear labeling.
- Add validation checks before accepting uploaded files in future versions.
